# Project-specific configuration
PROJECT_NAME=project1
REPO_URL=git@github.com:your-org/project1-repo.git
PROJECT_DIR=/opt/git-docker-compose-monitor/projects/project1
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-token

# How often (in minutes) to force pull floating tag images (latest, develop, etc.)
# Set to 0 to disable floating tag image checks entirely.
FLOATING_IMAGE_PULL_INTERVAL_MINUTES=60
